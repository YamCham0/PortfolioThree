{
  "name": "3d",
  "version": "0.0.0",
  "homepage": ".",
  "engines": {
    "node": "18.x"
  },

  "scripts": {
    "dev": "vite",
    "heroku-postbuild": "npm run build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.15"
  },
  "dependencies": {
    "three": "^0.136.0"
  }
}
