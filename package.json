{
  "name": "3d",
  "version": "0.0.0",
  "homepage": ".",
  "engines": {
    "node": "18.13.0"
  },

  "scripts": {
    "dev": "vite",
    "heroku-postbuild": "npm run build",
    "build": "build",
    "preview": "vite preview"
  },
  "devDependencies": {
  },
  "dependencies": {
    "three": "^0.136.0"
  }
}
